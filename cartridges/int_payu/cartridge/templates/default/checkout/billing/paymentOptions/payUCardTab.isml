<li class="nav-item active-tab" data-method-id="${paymentOption.ID}">
<iscomment> ${pdict.order.billing.payment.selectedPaymentInstruments[0].paymentMethod === paymentOption.ID ? 'active-tab' : ''} </iscomment>
    <a class="credit-card-tab checkboxPayUCard enabled-check arrows-accordion-payment arrows-accordion-payment-mobile collapsed ${paymentOption.ID + "-TAB"} ${paid}" data-toggle="collapse" href="#credit-card-content" >
        <div class="payu-payments payu-payment-card">
            <img class="payment-image" src="${URLUtils.staticURL('/images/credit_debit_card.png')}" alt="${paymentOption.name}"
                title="${paymentOption.name}" />
        </div>
    </a>
</li>